# Copy this file to .env and set values locally if you need them.
# For Netlify/Vercel, set the same variable in the project's environment variables.

# Backend API base URL used by the frontend
FRONTEND_API_URL=http://localhost:5000
